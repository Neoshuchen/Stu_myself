# 课程内容质量与后续改进

> 文档核对日期：2026-09-06；课程正文与 MySQL 全量审核日期：2026-09-04；学习增强回归日期：2026-09-05。
> 文档性质：当前仓库课程事实、仍有效的质量边界和后续优先级。本轮没有重新访问课程外部资料或重做数据库全量审核。

## 当前基线

- 唯一版本化课程源是 `backend/data/system_roadmaps.v2.json`，`schema_version=2`、`content_version=8`。
- 目录包含 13 条系统路线、379 个连续学习日：一条 42 日采集/Web/Android 综合路线，十一条 30 日专项路线，以及一条 Git 7 日路线。
- 运行时权威来源是 MySQL；JSON 只由 `import_curated_roadmaps` 校验并幂等导入。管理员人工编辑过的学习日默认不会被再次导入覆盖。
- 当前导入和测试会校验路线分类、连续日编号、必填内容、公开资料地址及内部来源泄漏；缩短路线时若待删除学习日已人工编辑或已有进度、讨论、建议，事务会中止。学习页面的完成门禁仍要求全部检查项和至少一份证据。

## 已完成治理

- 课程正文已统一到内容版本 8，并保持原路线标识及已有学习进度关联。
- 11 条 60 日路线按原阶段合并相邻主题为 30 日，避免用重复的讲解、练习和验收人为拉长周期；原有主题仍保留在合并后的核心知识与连续任务中。
- 原 182 日路线收敛为 42 日，只保留授权采集、Web 定位与还原、Android 静态/动态/Native 分析，删除与路线名称不一致的平台化、AI 和求职扩张。
- 知识详情的摘要、用途、基础知识、运行机制和课程作用改为职责不同的文本；未命中的并列术语只生成一条主题导读，不再逐词复制通用模板。
- 内容版本 8 为已审核概念保留独立解释，未单独收录的并列术语每日最多合并成一条应用导读；机制、工具、易错点和练习步骤全部绑定当天任务与验收，不再沿整条路线复制同一模板。
- 修复了数据库中 `crawler/javascript/android/ios` 等紧凑轨道标识被当成未知路线的问题，示例、工具和资料现在由真实轨道类型统一选择。
- 把 `/`、`+` 视为术语内部字符，修复 `I/O`、`/status`、`/model`、`/login` 等被错误拆分的内容；概念匹配改为最长关键词优先，避免 `Java.use` 误命中普通 Java 基础说明。
- 已按 30 日实际节奏换算阶段前置条件，并合并生活路线中每个子任务都重复的验收后缀。
- 已核对并修正 Cline 当前 IDE/CLI 安装口径与 Node.js 要求、Copilot CLI 版本命令、Gemini Checkpointing/`/restore`/`/rewind` 差异、MD5 安全边界、Base64 非加密属性和 ReAct 审计记录边界。

## 逐路线知识审核结论

| 路线 | 当前节奏 | 知识递进与本轮校正 | 结论 |
|---|---:|---|---|
| 采集/Web/Android 综合 | 42 天 / 93 小时 | Python 与 HTTP 基线后进入 Scrapy、浏览器定位、AST、运行环境还原，再进入 Android 静态、Java Hook、网络链和 JNI；修正 Day 4 的“未来 24 周”和 Day 36 的“第 10 周”失真时间引用 | 适合已有 Python/JS 基础者密集进阶，不作为零基础入门 |
| Python 基础 | 30 天 / 73.5 小时 | 语法与容器、函数与文件、对象协议、标准库与质量、并发/数据/网络、项目连续递进；数据库日只保留 SQLite、参数化 SQL 与事务，HTTP 日只保留客户端与 JSON 边界，删除 MySQL、Socket 等无关堆叠 | 主线完整，任务能够从可运行代码过渡到独立项目 |
| 爬虫基础 | 30 天 / 73.5 小时 | HTTP 快照、可靠请求、解析规范化、动态页面、并发恢复、Scrapy 工程化；开发者工具日分别解释 Headers、Payload、Preview，Scrapy 项目与 Pipeline 分日验收 | 技术层级清楚，所有案例限本地或授权目标，不教授规避授权 |
| JavaScript 逆向 | 30 天 / 73.5 小时 | 语言与异步、编码与密码学边界、DevTools、Hook、AST、环境依赖、跨语言对拍；拆开函数/闭包/`this` 与模块主题，移除具体商业站点和绕过式案例 | 可形成固定样本上的定位与复现能力，不承诺绕过任意真实系统 |
| Android 逆向 | 30 天 / 73.5 小时 | 授权实验室、Java 阅读、APK/DEX 静态分析、Frida Java、JNI/ELF/ARM64、故障诊断、结业证据链；环境日不再提前塞入代理/证书，故障日分别聚焦 ClassLoader/重载与多进程 | 需具备 Java 阅读能力并使用本人或书面授权 APK；进阶跨度合理 |
| iOS 逆向 | 30 天 / 73.5 小时 | 签名与安装、ObjC/Swift、IPA/Mach-O、LLDB、Frida、网络/存储、端到端报告；将环境验收、静动态证据链和结业提取任务收敛为单一成果 | 需 macOS/Xcode 和语言阅读基础；设备与签名差异必须现场验证 |
| AI Agent 工程 | 30 天 / 73.5 小时 | 最小循环、工具调用、状态与图、记忆/RAG、MCP/A2A、安全、评估、研究/编码 Agent、综合项目；删除 SFT/LoRA/GRPO/Agentic RL 训练岔路，补足回归评估与恢复 | 聚焦“能构建、评估和约束 Agent”，比模型训练混合路线更可完成 |
| Coding Agent 工具链 | 30 天 / 73.5 小时 | 通用权限与 Git 基线后学习 Codex、Claude Code、OpenCode、Gemini、Copilot、Aider、Cline、goose，再做统一指令、迁移和双工具基准；更新 Cline、Copilot、Gemini 当前事实，并把八工具同日比较缩为两个候选 | 安装、只读探索、受控修改、diff/测试/恢复闭环完整；产品命令仍应以当日官方文档为准 |
| 个人财务与风险管理 | 30 天 / 60 小时 | 财务快照、现金流、储备与债务、保险合同与反诈、长期投资原则、年度系统；合并收入/支出/账单日历，产品事实页与投资规则各自验收 | 提升个人财务判断与流程能力，不构成产品、税务或投资建议 |
| 健康管理与应急准备 | 30 天 / 60 小时 | 健康基线、睡眠、饮食、活动、压力与就医、急救边界、家庭预案；测量误差与健康主张同日核验，急救日按现场安全/呼救/CPR 与出血/气道/烧烫伤分层 | 能提升日常管理和求助准备，不替代诊疗或线下认证急救培训 |
| 沟通表达与问题解决 | 30 天 / 60 小时 | 倾听与复述、提问表达、边界反馈、冲突协商、论证决策、协作交付；把倾听与三层复述、主张证据推理、问题到小实验分别整合 | 每天有低风险演练与可观察反馈，避免只背沟通术语 |
| 数字安全与信息素养 | 30 天 / 60 小时 | 账号恢复、设备网络、钓鱼备份、横向阅读、图像视频与图表核验、生成式 AI、事件响应；唯一口令/MFA/恢复码和 AI 引用/隐私/伪造分层验收 | 覆盖个人数字生活主要高频风险，实验限定本人账号和离线样例 |
| Git 全流程 | 7 天 / 24 小时 | 对象与三区、差异历史、分支合并、远程协作、撤销恢复、rebase/cherry-pick、bisect/worktree；危险操作要求先预测、演练退出路径并从 `reflog` 恢复 | 强度较高但符合 7 日密集目标，能覆盖日常开发和常见事故恢复 |

## 2026-09-04 MySQL 实数据审核记录

以下数量及数据不变结论属于当次导入前后的快照，不能当作今天数据库的用户数或进度数。

- 本轮导入前直接读取 MySQL 中 13 条系统路线和 379 条 `PlanDay`，全部为内容版本 7；同时读取 6 条报名、7 条学习进度、3 条学习证据和人工编辑标记，不以 JSON 推测数据库状态。
- 对 379 天的标题、核心知识、任务、验收、知识详情、参考案例与资料映射做全量检查，并逐路线人工复核先修关系、每日负荷、工具时效性、授权和专业边界。
- V8 导入后再次直读 MySQL：13 条路线共 379 个学习日，全部 `content.version=8`；6 条报名、7 条学习进度、3 条学习证据的主键、关联、状态和附件路径未变。
- 每条路线中每日机制、易错点和练习步骤的唯一数均等于路线天数；各路线有 5～16 组日级参考资料组合，Git 7 天为 7 组，已消除整条路线复制同一详情模板的问题。
- 120 个去重参考链接以公开官方文档、标准组织、政府与专业机构为主；对 Python/Requests/Scrapy/Playwright、MDN/Chrome/Babel、Android/Apple/LLDB/Frida、Git、MCP/A2A 及各 Coding Agent 的易变事实重新核对。
- 全部 Python 与 JavaScript 参考代码完成语法检查，目录结构与内容字段检查为 0 项错误；示例只是最小可验证骨架，不冒充完整项目答案。
- 已删除课程输出中的内部资料地址，学习者资料只允许公开来源。
- 知识卡具备概念、机制、常见错误、最小实验、参考代码、预期结果、练习与掌握标准等结构。
- 对采集和逆向主题明确限定本人资产、专用靶场或已授权样本，不把 `robots.txt`、可访问页面或技术可行为视作授权。
- 课程导入、管理员正文保护和强制覆盖路径已有自动化测试。

## 仍需解决的质量边界

1. **实验资产仍需扩展**：Python Day 2、3、6 已提供三份仅依赖标准库的可下载案件，包含故障代码、断言和修复线索。仓库仍没有课程专用 APK、iOS App、本地 HTTP 靶场或 Babel 实验工程；涉及这些目标的学习日需由学习者提供合法环境。
2. **依赖仍分散在正文**：当前目录仍包含 Babel、pytest 等工具说明和占位路径。上线固定实验前，应为每条路线提供独立、锁定版本的实验依赖和预检命令。
3. **掌握度主要依赖自评与同伴证据**：Python Day 2/3/6、HTTP Day 2、Git Day 1 已加入 15 道服务端判分的闭卷题；其余主题继续使用闭卷自评。实验在本地运行，网站不执行任意学习者代码。
4. **内容变更缺少版本差异和回滚**：`content_edited_at` 可以保护人工修改，却不能向在学用户展示版本差异，也没有课程级一键回滚。
5. **移动端结论需要真实设备复核**：Android/iOS 路线涉及系统版本、签名、设备状态和工具版本差异，不能仅凭静态课件保证每个实验成立。

## 课程、固定练习与历史课件的关系

| 内容 | 权威位置 | 更新影响 |
|---|---|---|
| 系统课程版本化目录 | `backend/data/system_roadmaps.v2.json` | 仅在执行导入时进入数据库，修改文件不直接更新运行页面 |
| 学习者实际看到的正文 | MySQL `PlanDay` | 管理员保存后生效；`content_edited_at` 保护日级人工修改 |
| 客观三问与下载案件 | `backend/learning/study/practice.py` | 代码发布后按 slug、日编号和主题精确匹配，不经 JSON 导入 |
| 已提交的作答与解析 | MySQL `ReviewAttempt.quiz_results` | 保留答题时快照，后续题库改动不重算历史 |
| `docs/详细学习课件/01`～`11` | 内容版本 5 的 60 日历史快照 | 仅供追溯编排依据，不导入，不代表当前天数 |

当前专项路线仍保留原 `*-60d` slug 以保护关联，不能仅凭 slug 改写页面显示的天数或迁移已有报名。维护时同时检查目录的 `total_days`、实际连续日编号和目标数据库内容；Fork 或自建路线也不会因为标题相近而自动继承固定练习。

### 最小维护流程

1. 确定改动对象：一次在线正文修订使用管理员编辑器；系统课程统一升级使用版本化 JSON；客观题与实验使用 `practice.py`。
2. 修改前核对对应学习日、前后衔接、调用方与测试。主题变化后确认题目仍适用，否则接受固定题目不再展示，不能强行沿用旧答案。
3. 课程源更新按 `import_curated_roadmaps --slug 路线标识` 控制范围。该命令不是预览，会实际写入数据库；日级人工编辑默认跳过，但路线标题等元信息仍更新。
4. `--force` 会覆盖人工编辑正文并清除对应人工编辑标记；没有版本回滚功能，应先保留可恢复备份。存在关联数据的多余学习日仍不能直接删除。
5. 验证结构校验、保留关联、界面显示和直接相关测试；新增案件同时验证原始故障与参考修复输出，再更新本文覆盖范围。

知识花园、复杂赛季、自动生成题库、任意代码判题和公开成果展柜均不属于已实现功能。六项学习增强的操作、数据导出与验收见 [学习增强使用与验收](./学习增强使用与验收.md)。

## 后续优先级

| 优先级 | 工作 | 完成标准 |
|---|---|---|
| P0 | 固定授权实验资产与路线级依赖 | 本地 HTTP/JS 样例及授权移动端样本有版本、校验和、安装步骤和标准输出 |
| P0 | 内容版本差异与回滚 | 管理员可查看操作者、变更字段和版本，并可恢复上一稳定版本 |
| 已有首版 | 少量高价值客观测验 | Python、HTTP、Git 五个主题已覆盖，后续按实际错题和反馈扩充 |
| P1 | 课程质量看板 | 能看到卡点日、退出位置、重复缺口和反馈处理状态，不公开个人明细 |
| 已完成 | Markdown/AI 路线生成 | 首版生成可诊断、可编辑的私有骨架草稿，复用现有配置、结构校验和管理员审核，不直接发布 |

## 不采用的扩充

- 不用新增更多长路线掩盖实验不可复现的问题。
- 不为一份路线再建第二套课程模型、导入状态机或发布流程。
- 不执行模型生成的代码，也不让 AI 结果绕过人工确认和管理员审核。
- 不使用登录积分、全站排行榜或打卡次数代表掌握程度。

已实现的 AI 课程生成边界见 [`AI辅助Markdown学习路线生成规划`](./AI辅助Markdown学习路线生成规划.md)；规模化运营触发条件见 [`第四阶段-上线前规模化能力开发规划`](./第四阶段-上线前规模化能力开发规划.md)。
